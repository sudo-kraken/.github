## Endpoints

{%- if endpoints|default(false) %}
{%- for ep in endpoints %}
- `{{ ep }}`
{%- endfor %}
{%- else %}
No endpoints are declared by the repository automation. Document the public
interface here when it is available.
{%- endif %}
