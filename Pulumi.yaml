name: .github
runtime:
  name: python
  options:
    toolchain: uv
description: Github meta repository, contains automation/workflows/standard configs used across my repositories.
config:
  pulumi:tags:
    value:
      pulumi:template: github-python
  github:owner: sudo-kraken
