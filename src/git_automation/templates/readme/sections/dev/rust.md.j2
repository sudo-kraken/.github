Install [rustup](https://rustup.rs/).

{%- if lint|default(false) %}
### Run linter and formatter

```
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
```
{% endif %}

{%- if test|default(false) %}
### Run test

```
cargo test
```
{%- endif %}
