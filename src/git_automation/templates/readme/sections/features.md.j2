## Features

  - {{ feature_1 | default('Simple HTTP endpoints') }}
  - {{ feature_2 | default('Health endpoint for orchestration') }}
  - {{ feature_3 | default('Configurable via environment variables') }}
