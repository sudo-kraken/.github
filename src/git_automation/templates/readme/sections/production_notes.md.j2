## Production notes

- Pin deployments to an immutable release or image digest.
- Configure resource limits, restarts, timeouts, and logging for the target environment.
- Connect liveness and readiness probes only to endpoints the application implements.
