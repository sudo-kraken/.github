## Architecture at a glance

- Primary implementation: {{ language | capitalize }}
{% if package -%}
- Package distribution is enabled.
{%- endif %}
{% if binary and package -%}
- Release binaries are built for the configured target platforms.
{%- endif %}
{% if docker -%}
- A container image is built for the configured target platforms.
{%- endif %}
- Runtime entry points and internal components are documented alongside the source code.
