---
# THIS FILE IS GENERATED! DO NOT EDIT! Maintained by Pulumi
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Create pull request for automation-sync
on:
  push:
    branches:
      - {{ branch_name }}
permissions: {}
concurrency:
  group: automation-sync-pr-{{ "${{" }} github.ref {{ "}}" }}
  cancel-in-progress: true
jobs:
  create-pull-request:
    name: Create Pull Request
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout code
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1
        with:
          persist-credentials: false

      - name: Check for differences with {{ default_branch_name }}
        id: check-diff
        run: |
          git fetch origin {{ default_branch_name }}
          DIFF=$(git diff origin/{{ default_branch_name }} --name-only)
          if [ -z "$DIFF" ]; then
            echo "has_diff=false" >> $GITHUB_OUTPUT
          else
            echo "has_diff=true" >> $GITHUB_OUTPUT
          fi

      - name: Create Pull Request
        if: steps.check-diff.outputs.has_diff == 'true'
        run: "gh pr view {{ branch_name }} && gh pr reopen {{ branch_name }} || gh pr create -B {{ default_branch_name }} -H {{ branch_name }} --title 'chore(git-sync): auto-applied' --body 'This PR is auto-generated by https://github.com/{{ repository_name }}.'"
        env:
          GITHUB_TOKEN: {{ "${{" }} secrets.GITHUB_TOKEN {{ "}}" }}
